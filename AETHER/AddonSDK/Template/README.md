# AETHER Addon Template

1. Replace IDs/names in `addon.json`
2. Edit `panel.json` + `ui/layout.json`
3. Add optional preset bank
4. Run `../../scripts/validate_addon.py .`
5. Package with `../../scripts/package_addon.py . MyAddon.zip`
